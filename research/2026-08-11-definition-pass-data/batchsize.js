export const meta = {
  name: 'batchsize',
  description: 'Does batch size inflate agreement? 40 items rated in batches of 10 vs all at once',
  phases: [{ title: 'Rate', detail: '8 raters x 4 batches of 10, plus 8 raters on all 40' }],
}
const BOOK = "### 3.2 The fifteen definitions\n\n---\n\n#### `observation` \u2192 `case` \u00b7 anchor \u03ba 0.79\n\n> **Cue.** **Insight gleaned from events** \u2014 a pattern, inference or reading\n> drawn from what happened, but not definitive enough to be a rule or a\n> recommendation. Anecdotal by nature: it holds so far, on what has been seen.\n>\n> **Excludes:** the occurrence itself, however quantified (\u2192 `event`); an\n> established difference between two options (\u2192 `distinction`); reasoning from\n> fundamentals (\u2192 `principle`); something definitive enough to be followed\n> (\u2192 `procedure`, `obligation`, `prohibition` or `recommendation`).\n>\n> **Strip test.** An empirical result is an `observation` *even when the author\n> generalizes from it*. Delete the numbers and the sample from the sentence: if\n> nothing of substance survives, it was an `observation`; if a causal claim\n> survives on its own, it is a `principle`.\n>\n> **Doc.** \"Realized volatility on the book exceeded the model's 99th-percentile\n> band on three consecutive sessions in March 2026.\"\n> **Chat.** \"loss spiked right after we bumped LR to 3e-4, twice in a row\"\n\n#### `event` \u2192 `case`\n\n> **Cue.** **What happened.** An occurrence, with a time and an actor or\n> subject. Quantity does not disqualify it \u2014 an incident report full of counts\n> and losses is still a record of what happened.\n>\n> **Test.** Is the payload the *occurrence*, or an *inference drawn across\n> occurrences*? \"Between 13:42 and 13:58 the strategy quoted off a stale book in\n> 214 symbols, 3,118 executions, $2.3m loss\" is an `event`. \"Third timeout this\n> week, same node every time \u2014 the nic is cooked\" reads the events and is an\n> `observation`.\n>\n> **Excludes:** a reading drawn from what happened (\u2192 `observation`); a settled\n> choice (\u2192 `decision`); a generally accepted state of affairs (\u2192 `background`).\n>\n> **Doc.** \"The prime broker raised margin requirements on the fund's short book\n> on 14 March 2026.\"\n> **Chat.** \"training run 47 OOM'd overnight on node 3\"\n\n\n---\n\n#### `obligation` \u2192 `method`\n\n> **Cue.** **What to do** \u2014 a requirement someone is accountable to. The\n> positive counterpart of `prohibition`.\n>\n> **Judge the action, not the grammar.** A requirement stated with a negation is\n> still an obligation if what it demands is an action: *\"nothing reaches an\n> exchange that has not passed through the combiner\"* requires passing through\n> the combiner \u2014 `obligation`, not `prohibition`.\n>\n> **Excludes:** what must NOT be done (\u2192 `prohibition`); advice with no\n> accountability (\u2192 `recommendation`); a choice reported as taken rather than a\n> standing requirement (\u2192 `decision`).\n>\n> **Doc.** \"Positions must be marked to market daily before 17:00 ET.\"\n> **Chat.** \"every eval run has to log its seed and commit hash\"\n\n#### `prohibition` \u2192 `method`\n\n> **Cue.** **The negative case of `procedure`** \u2014 where a `procedure` says what\n> to do, a `prohibition` says what must not be done. A hard rule, manual-grade:\n> it would be printed as an absolute and it can be *violated*.\n>\n> **Test.** Could someone be in breach of it? A `prohibition` was set by someone\n> with standing, and doing it anyway is a breach. A peer pointing away from\n> something out of experience \u2014 *\"dead end, don't redo it\"* \u2014 creates no breach\n> and is a `recommendation`, however imperative it sounds.\n>\n> **Excludes:** a positively-stated requirement (\u2192 `obligation`); advice against\n> an action with no accountability behind it (\u2192 `recommendation`).\n>\n> **Doc.** \"The desk may not carry overnight exposure in names below $50m ADV.\"\n> **Chat.** \"never train on anything that overlaps the eval split\"\n\n\n#### `decision` \u2192 `method`\n\n> **Cue.** A choice reported as settled, which governs what happens after it.\n> Typically past-tense and agentive: *adopted*, *chose*, *approved*,\n> *standardised on*, *agreed to*. This is the one `rule` label with no deontic\n> modal to key on.\n>\n> **Excludes:** something that merely occurred, with no choice made (\u2192 `event`);\n> a choice still being proposed (\u2192 `recommendation`); a statement carrying an\n> explicit modal, which the deontic labels take instead.\n>\n> **Doc.** \"The committee standardised on daily rebalancing after the turnover\n> analysis.\"\n> **Chat.** \"we went with LoRA instead of a full fine-tune\"\n\n---\n\n#### `procedure` \u2192 `method` \u00b7 anchor \u03ba 0.74\n\n> **Cue.** An established operational instruction: **how the system is used.**\n> It could be pasted into a user manual unchanged and read as something to be\n> followed every time.\n>\n> **Test.** Is the sentence *reporting practice* or *putting something forward*?\n> Reporting is `procedure`: *we use*, *backtests are charged*, *the loader runs\n> at 07:00*, *positions are marked daily*.\n>\n> **Excludes:** anything put forward rather than reported \u2014 *we propose*,\n> *proposal:*, *we recommend*, *is preferable*, *should*, *worth*, *what if*,\n> *try* (\u2192 `recommendation`); anything hedged \u2014 *I think*, *probably*,\n> *usually*, *not sure* (\u2192 `recommendation`); anything announced as a\n> contribution to a discussion \u2014 *unpopular take*, *cheapest idea*,\n> *counterpoint* (\u2192 `recommendation`); how the system is **built** rather than\n> used (\u2192 `architecture`); a settled configuration choice among real\n> alternatives (\u2192 `decision`); the arithmetic defining a quantity (\u2192 `formula`);\n> a constraint someone is accountable to (\u2192 `obligation`).\n>\n> `[MEASURED]` These were two labels (`procedure` and `technique`) until the\n> second collision test measured `technique` at \u03b1 0.588, the weakest in the set.\n> Merged.\n>\n> **Doc.** \"To build the factor: winsorize at 1%, z-score cross-sectionally,\n> then neutralize by sector and size.\"\n> **Chat.** \"we use gradient checkpointing to fit the batch on one node\"\n\n\n#### `recommendation` \u2192 `method`\n\n> **Cue.** Advice on what ought to be done, with no requirement force and no\n> report that it was settled. Hedged: *should*, *prefer*, *is worth*, *probably\n> want to*.\n>\n> **Excludes:** a requirement someone is accountable to (\u2192 `obligation`); a\n> choice already made (\u2192 `decision`); a bare description of an approach (\u2192\n> `procedure`).\n>\n> **Doc.** \"Practitioners should prefer shrinkage estimators when the sample\n> covariance matrix is near-singular.\"\n> **Chat.** \"you probably want to warm up the LR over the first 2k steps\"\n\n---\n\n#### `definition` \u2192 `concept` \u00b7 anchor \u03ba 0.81\n\n> **Cue.** Fixes what a term means. The grammatical centre is *X is / means /\n> refers to / is defined as Y*.\n>\n> **Excludes:** a contingent statement that could turn out false, which fixes no\n> terminology (\u2192 `principle`); a contrast drawn between two terms (\u2192\n> `distinction`).\n>\n> **Doc.** \"The Sharpe ratio is excess return divided by the standard deviation\n> of excess return.\"\n> **Chat.** \"perplexity is just exp of the mean negative log-likelihood\"\n\n#### `distinction` \u2192 `concept`\n\n> **Cue.** A **qualifier between multiple methods or options**, resting on\n> established differences rather than on one occasion's measurement. The\n> contrast is the payload: two things are named and what separates them is the\n> point.\n>\n> **Proven, not anecdotal.** A `distinction` cites differences that hold \u2014 fee\n> schedules, contract terms, published behaviour. If the contrast rests on what\n> someone saw once, it is an `observation`.\n>\n> **Excludes:** a single term being defined (\u2192 `definition`); a theoretical\n> claim reasoned from fundamentals (\u2192 `principle`); a one-off comparison\n> someone happened to measure (\u2192 `observation`).\n>\n> **Doc.** \"Realized volatility is measured from past returns; implied\n> volatility is backed out of option prices.\"\n> **Chat.** \"RAG retrieves at query time; fine-tuning bakes it into the weights\"\n\n#### `background` \u2192 `concept` \u00b7 anchor \u03ba 0.87\n\n> **Cue.** **Historical enrichment.** Context that deepens a reader's\n> understanding of how things came to be as they are. It explains \u2014 but unlike\n> `principle`, `procedure`, `obligation` or `prohibition`, **it defines\n> nothing**: no rule to follow, no standing relation, no way of working.\n>\n> **It is contextual, and usually announced.** Background is normally signalled\n> by the writer or by where it sits \u2014 *for context*, *historically*, *the desk\n> began in*, the opening chapter of a handbook. Decide from the surrounding\n> context (\u00a71): the same sentence is `background` in an orientation chapter and\n> an `event` in an incident timeline.\n>\n> **Against `observation`.** An `observation` reads events and draws something\n> from them. `background` does not read events \u2014 it supplies the surround.\n>\n> **Last resort.** `background` loses every tie. If any other test fires, that\n> label wins \u2014 a dated change is an `event`, a term being fixed is a\n> `definition`, a contrast is a `distinction`, an explanation is a `principle`.\n> `background` is what remains when a statement informs but claims nothing,\n> instructs nothing, and defines nothing. `[MEASURED]` It was previously written\n> as a positive category \u2014 \"context a reader needs, presented as generally\n> accepted\" \u2014 which describes a large share of all expository prose, and it\n> collided with `event` 11 times, `obligation` 4 and `principle` 4 on the first\n> corpus that exercised it.\n>\n> **Excludes:** everything else. That is the point.\n>\n> **Doc.** \"Cross-sectional momentum has been documented in equity markets since\n> Jegadeesh and Titman.\"\n> **Chat.** \"transformers displaced RNNs for sequence modelling around 2018\"\n\n---\n\n#### `principle` \u2192 `model`\n\n> **Cue.** A **theoretical approach** \u2014 reasoning from fundamentals about why\n> something holds. First principles, not comparison and not measurement.\n>\n> **Test.** Would it survive without the specific case in front of you? A\n> `principle` is stated at the level of the idea. A statement that compares two\n> named options is a `distinction`; one that reports what was seen is an\n> `observation`, even when it explains itself.\n>\n> **Excludes:** how the thing is built or sourced (\u2192 `architecture`); the equation\n> that computes it (\u2192 `formula`); what must be true for it to hold (\u2192\n> `assumption`); what it needs in order to run (\u2192 `dependency`); a single\n> measured occasion (\u2192 `observation`).\n>\n> **Carries `status`** (\u00a72.6), as every type does: `floated` when raised but not\n> worked out, `proposed` when specified but unvalidated, `evidenced` when\n> something backs it, `settled` when indisputable. The status is not part of\n> choosing this label \u2014 a principle is a principle whether or not it is proven.\n>\n> **Doc.** \"Parking-lot vehicle counts predict same-store sales, so changes in\n> counts lead reported revenue by roughly one quarter.\"\n> **Chat.** \"the whole thesis is more cars means the store is doing better\"\n\n#### `architecture` \u2192 `system`\n\n> **Cue.** The **construction of a physical or software system** \u2014 what it is\n> built out of, what components it has, what they are. Not a logical scheme and\n> not a configuration: an actual system, described structurally.\n>\n> **The elevator test.** What the doors are made of and how the hoist is\n> assembled is `architecture`. How to call the car is `procedure`.\n>\n> **Actual, not proposed.** `architecture` describes a real implementation \u2014\n> which includes a specification of one that has been committed to. A system\n> being *put forward* is a `recommendation`, however concrete its detail.\n>\n> **Excludes:** how the system is *used* (\u2192 `procedure`); a settled setting or\n> configuration choice (\u2192 `decision`); why it works (\u2192 `principle`); the\n> arithmetic (\u2192 `formula`); something separate the build needs present\n> (\u2192 `dependency`).\n>\n> **Doc.** \"Parking-lot counts are derived from daily satellite imagery, matched\n> to store locations by geofence and aggregated to ticker level.\"\n> **Chat.** \"signal's built from the vendor feed plus our own geofences\"\n\n#### `formula` \u2192 `model`\n\n> **Cue.** **Defines a quantity** by giving the arithmetic that produces it. A\n> `formula` says what a number *equals*; it instructs nobody.\n>\n> **No opinions in a formula.** A formula computes and stops. If the sentence\n> also evaluates, judges or hedges the result \u2014 *small but it doesn't look like\n> plain momentum* \u2014 that part is not formula, and a statement carrying both is\n> doing two jobs (\u00a72.4).\n>\n> **Test.** Is the arithmetic the payload, or machinery inside a policy? \"OCC\n> divides the strike by four and multiplies contracts by four\" defines the\n> adjustment \u2014 `formula`. \"The book targets 10% vol on a 0.94-decay covariance;\n> if realized exceeds 13% the scaler cuts gross\" uses arithmetic to state what\n> is done \u2014 `procedure`.\n>\n> **Excludes:** a term's meaning with no computation given (\u2192 `definition`); a\n> reported value rather than the rule producing it (\u2192 `observation`); arithmetic\n> in service of an operational instruction (\u2192 `procedure`).\n>\n> **Doc.** \"Signal = z-score of the trailing 30-day change in vehicle count,\n> winsorized at 1% and neutralized by sector.\"\n> **Chat.** \"it's just zscore(30d delta) then sector demean\"\n\n#### `assumption` \u2192 `model`\n\n> **Cue.** States something taken for granted for the thing to hold. Marker\n> words: *assumes*, *presupposes*, *conditional on*, *holds only if*, *provided\n> that*.\n>\n> **Excludes:** something needed to run rather than to be true (\u2192 `dependency`);\n> a limit on where it applies, stated as a result (\u2192 `observation` or `principle`);\n> a rule someone must follow (\u2192 `obligation`).\n>\n> **Doc.** \"The signal assumes foot traffic converts to revenue at a stable rate\n> across store formats.\"\n> **Chat.** \"this only works if the conversion rate is roughly constant\"\n>\n> `[DESIGN]` High value for this corpus specifically: quantitative models fail\n> at their assumptions far more often than at their arithmetic, and assumptions\n> are usually the least recorded part of a model.\n\n#### `dependency` \u2192 `system`\n\n> **Cue.** **An established requirement** \u2014 a hard fact that the thing cannot\n> run without. Marker words: *requires*, *depends on*, *needs*, *is a\n> prerequisite for*, *without X you cannot*.\n>\n> **Hard, not casual.** A `dependency` holds as a matter of fact about the\n> system. Someone noting that a piece of data would be handy, or that they wish\n> a mapping existed, is reporting their situation \u2014 `observation`, which is\n> anecdotal by nature.\n>\n> **Excludes:** something taken for granted for a claim to hold (\u2192 `assumption`);\n> what the thing is built from (\u2192 `architecture`); an anecdotal note about what\n> is missing (\u2192 `observation`).\n>\n> **Doc.** \"Signal construction requires the earnings calendar in order to\n> suppress positions into scheduled announcements.\"\n> **Chat.** \"needs the earnings cal, otherwise we trade straight into prints\"\n\n\n\n\n### 3.3 Pairwise separations\n\n`[VERIFIED]` Pairwise distinction rules are the bulk of a working codebook, not\na garnish: the scheme that reached \u03ba 0.71 shipped **75** of them alongside a\ndecision tree, in 111 pages of guidelines.\n\n`[DESIGN]` The nineteen rules below cover the pairs judged to genuinely collide.\nPairs not listed were judged non-colliding \u2014 that judgment is mine and is worth\nchallenging; a pair that turns out to collide in practice should be added here\nrather than patched into a definition.\n\n| Pair | The test that separates them |\n|---|---|\n| `observation` / `event` | Was anything measured? An observation records a quantity or behaviour; an event records that something occurred. |\n| `event` / `decision` | Was a choice made? An event happened to you; a decision was chosen and constrains later action. |\n| `decision` / `obligation` | Is there a modal? An obligation states a standing requirement in modal form; a decision states one was established. If both fit, the modal wins. |\n| `decision` / `recommendation` | Settled or proposed? A decision is closed; a recommendation is still advice. |\n| `obligation` / `prohibition` | **What to do** versus **what not to do.** Judge by the action demanded, not by grammatical polarity \u2014 a negatively-phrased requirement that demands an action is an `obligation`. `[MEASURED]` Both items in the 21-pair collision were compound, carrying a requirement and a forbidding in one sentence; under \u00a72.4 both tests score above 90 and the statement resolves to `general` with `multi_fire`. |\n| `background` / `event` | `[MEASURED]` 13 rater-pairs. Both report what happened; the difference is what the sentence is *for*. An `event` records an occurrence in its own right; `background` uses occurrences to explain how things came to be, defines nothing, and is normally signalled or positioned as context. Decide from the surrounding context, not the sentence alone. |\n| `recommendation` / `obligation` | Is anyone accountable? Advice can be ignored without violation; an obligation cannot. |\n| `principle` / `architecture` | `[MEASURED]` The largest collision in the second test (17 disagreeing rater-pairs, when these were `driver`/`structure`). WHY it works versus HOW it is built. If deleting the sentence would leave you unable to explain the idea, it is a principle; if it would leave you unable to rebuild the thing, it is architecture. |\n| `principle` / `obligation` | Explanatory or normative? A principle says why something holds; an obligation says someone must do something. \"Prefer small reversible steps\" is normative \u2014 it is a `recommendation` or `obligation`, not a principle. |\n| `principle` / `recommendation` \u2014 the theoretical/practical test | `[MEASURED]` 18 rater-pairs. Is it **theoretical or hands-on**? A `principle` is a general, logical guide that would hold for anyone, stated at concept level. A `recommendation` is practical advice drawn from experience, bearing on a choice at hand **where more than one valid option exists**, and it usually sounds conversational. *\"Counterpoint on freight \u2014 whatever we build there we're third in line behind people with faster feeds\"* states a fact but its job is to stop an action, and it is experiential and conversational: `recommendation`. |\n| `decision` / `procedure` | `[MEASURED]` 7 disagreeing rater-pairs in the third test, across two coarse types. `[DESIGN]` **Does the sentence name what was chosen *instead of* something else?** Surface cues: *rather than*, *not X but Y*, *instead of*, *we standardised on*. Naming the rejected alternative makes it a `decision`; stating only how the thing is done makes it a `procedure`. \"Models are versioned by artifact SHA-256, not by semantic version\" names the alternative \u2014 `decision`. |\n| `principle` / `assumption` | Is it the reason it works, or a precondition for it working? A principle explains; an assumption is what must hold for the explanation to survive. |\n| `principle` / `observation` | `[MEASURED]` The largest collision measured in any run (37 rater-pairs in the control, 17 after repair and renaming, 11 with the strip test). Apply the **strip test** from \u00a73.2 `observation`: delete the numbers and the sample from the sentence. If nothing of substance survives, it is an `observation` \u2014 *even if the author generalizes from it*. If a causal claim stands on its own without any measurement, it is a `principle`. `[MEASURED]` A second, scope-judging test was written for this boundary and measured worse (\u00a72.9); do not reintroduce one without re-testing. |\n| `architecture` / `formula` | Parts or arithmetic? Architecture names components; a formula computes a value. |\n| `architecture` / `dependency` | Inside or outside? Architecture is what the thing is made of; a dependency is something separate it needs. |\n| `assumption` / `dependency` | Must be TRUE, or must be PRESENT? An assumption is a belief the model rests on; a dependency is an input it consumes. |\n| `formula` / `definition` | Does it compute? A formula produces a number; a definition fixes a meaning. |\n| `definition` / `background` | Fixing a term or setting the scene? A definition pins meaning; background situates the reader. |\n| `definition` / `distinction` | One term or two? A definition fixes one; a distinction separates two. |\n| `obligation` / `procedure` | `[MEASURED]` A cross-coarse leak in the first test (6 disagreeing rater-pairs). A procedure tells you the steps; an obligation tells you that doing it is required. A numbered list with a *must* in it is an obligation. |\n| `procedure` / `definition` | `[MEASURED]` 15 rater-pairs, all on one naming-convention statement. Does the sentence describe **what a thing is**, or **what to do with it**? A naming scheme, a schema, a set of valid values is a `definition` even when following it requires action. |\n| `procedure` / `principle` | Is the payload the instruction or the reason? A `procedure` tells you what is done; a `principle` tells you why it holds. Where a sentence carries both, the instruction wins \u2014 unless it is hedged or reassuring, in which case see below. |\n| any / `recommendation` \u2014 the put-forward rule | `[MEASURED]` 15 rater-pairs on proposed architectures, and the dominant signal in the 72-pair `procedure` collision. **Anything proposed is a `recommendation`**, whatever it proposes. Markers: *we propose*, *proposal:*, *this RFC proposes*, *we recommend*, *is preferable*, *should*, *worth*, *what if*, *try*, *suggest*. Content does not override this \u2014 a fully specified system architecture that is being put forward is a `recommendation`, not an `architecture`. |\n| any / `recommendation` \u2014 the reassurance marker | `[DESIGN]` **Procedures and principles do not have feelings.** Language that soothes, warns off, or manages the reader's reaction \u2014 *don't panic*, *no need to worry*, *don't stress*, *ignore me* \u2014 marks the sentence as advice from a person, not an operational instruction or a standing relation. Route to `recommendation`. |\n\n---"
const STATUS = "### Status \u2014 how firmly the statement is held\n\nStatus is INDEPENDENT of type. It does not change what kind of statement\nsomething is; it records how far along it is. A `principle`, a `procedure` and\nan `architecture` can each sit at any rung.\n\nAssign exactly one rung per statement, or `n/a`.\n\n---\n\n#### `floated`\n\n> **Cue.** A point raised without being worked out. Options canvassed, nothing\n> specified. To act on it you would have to invent the details yourself.\n>\n> **Test.** Could you build or check this from the sentence as written? If you\n> would first have to decide the parameters, the data, or the mechanism, it is\n> `floated`.\n>\n> Example: \"what if we looked at parking lot traffic for the retail names\"\n> Example: \"there's probably something in the shipping data too, worth a poke\"\n\n#### `proposed`\n\n> **Cue.** One approach, specified enough to build or test. Nothing has\n> validated it yet.\n>\n> **Test.** Could you act on this as written? If yes, and no evidence is\n> offered, it is `proposed`.\n>\n> Example: \"weight each name by lot occupancy normalised to store footprint,\n> rebalanced weekly\"\n> Example: \"we should move the warehouse to point-in-time fundamentals\"\n\n#### `evidenced`\n\n> **Cue.** Something backs it, and the backing is what makes it hold \u2014 a\n> measurement, a backtest, an ablation, a cited result.\n>\n> **Test.** Is there evidence in or behind the sentence that could be wrong?\n>\n> Example: \"the lot-count signal held up out of sample, 0.41 Sharpe net\"\n> Example: \"ablations show the gain comes from recency weighting, not capacity\"\n\n#### `settled`\n\n> **Cue.** Indisputable. Not the kind of thing the next study overturns.\n>\n> **Test.** Would anyone competent argue with it?\n>\n> Example: \"Sharpe ratio is excess return divided by standard deviation\"\n> Example: \"US equity markets close at 16:00 Eastern\"\n\n#### `n/a`\n\n> Use when the rung genuinely does not apply \u2014 the statement is not the kind of\n> thing that gets more or less established. Do NOT use it because you are unsure\n> between two rungs."
const SCOPE = "### scope \u2014 how far the statement reaches\n- `instance` \u2014 one occasion, run or case.\n- `system` \u2014 true of our system, desk or codebase; would not transfer unchanged elsewhere.\n- `world` \u2014 true independent of us; another team would find the same."
const FORM = "### form \u2014 statement, question or answer\n- `statement` \u2014 asserts something. The default.\n- `question` \u2014 asks for something. Asserts nothing, defines nothing, instructs nothing.\n- `answer` \u2014 supplied in response to a question.\nA question still gets a type: it is typed by what it is ABOUT."
const ITEMS = [{"id": "postmortem-18", "text": "The kill switch performed exactly to specification, cancelling all 1,912 resting orders across CME, ARCA and EDGX in 310 ms with zero rejects."}, {"id": "comparison-memo-16", "text": "Either framework would be classified Tier 2 under our model risk policy, but replacing a validated production model restarts the validation queue; Model Risk indicated a 10\u201314 week turnaround, against a documented parameter change to the incumbent, which requires only a change memo."}, {"id": "ml-chat-15", "text": "chinchilla-optimal for 7b is around 140b tokens, we're doing 2T. that's deliberate \u2014 inference cost dominates for this one, we're overtraining on purpose"}, {"id": "reference-doc-8", "text": "**Curvature risk** \u2014 Under the FRTB standardised approach, the loss remaining beyond the delta approximation, measured by shocking each risk factor up and down by its prescribed delta risk weight and taking the worse outcome."}, {"id": "rfc-doc-15", "text": "Every ingest must emit an OpenTelemetry span carrying dataset.bytes, dataset.file_count, and instrument.serial, so that an event like the 11-hour backlog of 2026-05-19 can be attributed to a specific device instead of to \"the network.\""}, {"id": "research-results-7", "text": "Gradient-boosted regression trees deliver a monthly out-of-sample R\u00b2 of 0.41% for individual stock returns against 0.27% for OLS on the same 94 predictors; the corresponding value-weighted decile spread has an annualized Sharpe ratio of 1.35 before costs."}, {"id": "brainstorm-desk-1", "text": "the earnest panel on CAVA looks incredible until you plot panel size, it roughly doubles between 2023-02 and 2023-04. any spend growth before you normalize is just onboarding"}, {"id": "ml-chat-6", "text": "nccl timeout on node 17 again, 03:41 UTC. third one this week and it's the same node every time, i think the nic is cooked"}, {"id": "onboarding-handbook-8", "text": "Backtests are charged costs from the fitted impact model in costs/, a square-root law calibrated on our own fills since 2016; the ES coefficient implies roughly 0.4 ticks on a 500-lot at typical morning depth."}, {"id": "comparison-memo-19", "text": "We recommend adopting the second framework for curve construction and retaining the incumbent for swaption pricing, on the understanding that the supporting evidence is weaker than the exhibits imply: the backtest window includes the April 2025 volatility episode, and excluding those fifteen sessions reduces the measured advantage to within one standard error of zero."}, {"id": "rfc-doc-0", "text": "This RFC proposes retiring the six per-facility NFS landing zones in favor of a single S3-compatible ingest endpoint backed by MinIO, with POSIX mounts retained only for Titan Krios session directories until the EPU 3.8 upgrade lands in Q3 FY27."}, {"id": "ml-chat-5", "text": "run is `pretrain-7b-0812` on wandb if you want to watch it. val loss 2.187 at 45k"}, {"id": "comparison-memo-1", "text": "The two crude venues differ most at expiry rather than in continuous trading: ICE Brent settles financially against the Brent Index, whereas NYMEX WTI requires physical delivery at Cushing, and the desk has no capacity to manage delivery notices. On that basis alone Brent wins for any position we might carry into the last three trading days."}, {"id": "brainstorm-desk-10", "text": "counterpoint on freight, ODFL and SAIA already move on the ATA tonnage print and truckstop spot rates. whatever we build there we're third in line behind people with faster feeds"}, {"id": "ml-chat-17", "text": "muon on the 2d params, adamw on embeddings and the lm head. that's the split kimi used and it held up in our 1b ablation"}, {"id": "research-results-3", "text": "The variance risk premium, defined as VIX\u00b2 less the model-free realized variance of the subsequent 21 trading days, averages \u221217.6 annualized variance points between January 1990 and December 2022 and is negative in 74% of months."}, {"id": "postmortem-15", "text": "AI-4 (owner: M. Devlin, due 2026-08-29) \u2014 add a 25 bps NBBO-relative band to the risk gateway that hard-rejects outbound quotes crossing it, with a per-symbol override table requiring sign-off from the head of trading."}, {"id": "postmortem-8", "text": "Replay of the pcap from mdgw-prod-04 confirms a genuine 1,204-packet gap on the A feed at 13:42:11 ET; the gateway did request retransmission from the recovery channel, but the request timed out after 5s and was never retried."}, {"id": "research-results-0", "text": "In Fama\u2013MacBeth regressions over January 1972 to December 2023, the coefficient on idiosyncratic volatility (estimated from daily Fama\u2013French three-factor residuals over the prior month) is \u22120.42 with a Newey\u2013West t-statistic of \u22123.17 at four lags, implying a 51 bp monthly spread between the extreme quintiles."}, {"id": "comparison-memo-4", "text": "Venue primacy in FX remains pair-specific: USDJPY price discovery still concentrates on EBS Market, while EURGBP and AUDUSD reference LSEG FX Matching, so a single-venue mandate would degrade execution on roughly a third of our G10 flow."}, {"id": "reference-doc-0", "text": "**DV01** \u2014 The change in a position's present value for a one basis point parallel downward shift in the zero curve, reported in the position's settlement currency and signed so that a long bond carries positive DV01."}, {"id": "brainstorm-desk-13", "text": "glassdoor ceo approval, 6mo change, quintile spread was ~4.2% annualized 2015-2020 and flat since. textbook crowded-out alt data curve"}, {"id": "rfc-doc-8", "text": "Content-addressed deduplication using BLAKE3 over 64 KiB content-defined chunks is proposed for derived data only; a two-week trial measured a 1.06 ratio on raw detector output versus 2.31 on motion-corrected micrographs and coordinate-sorted BAMs."}, {"id": "rfc-doc-13", "text": "The Zeiss Lattice Lightsheet 7 and the timsTOF Pro 2 can write only to Windows SMB shares, so this RFC specifies a sidecar agent \u2014 a single static Go binary \u2014 that polls every 15 s and requires 60 s of file-size quiescence before beginning upload."}, {"id": "onboarding-handbook-9", "text": "We report deflated Sharpe ratios after Bailey and L\u00f3pez de Prado (2014), and you must declare the number of configurations tried in the trial log before presenting any result. Undeclared trials are the fastest way to lose credibility on this floor."}, {"id": "brainstorm-desk-16", "text": "note on the vendor file, it lands 6:15am ET but every row is stamped as-of prior close and our loader runs at 7. we're effectively t+2, which probably halves whatever IC we measure in research"}, {"id": "ml-chat-18", "text": "counterpoint to skipping the batch \u2014 if it's a real data issue we'll just hit it again at 60k with a different shard. i'd rather spend 4 hours finding it"}, {"id": "rfc-doc-6", "text": "We propose relaxing the catalog SLO for faceted search from p95 < 800 ms to p95 < 1.5 s over collections exceeding 10^7 datasets, since the current target is only met by pinning the entire facet index in memory, now 96 GB and growing at 4 GB per quarter."}, {"id": "reference-doc-14", "text": "Under Regulation T, initial margin on a long equity position is 50 percent of the purchase price; FINRA Rule 4210 sets the minimum maintenance requirement at 25 percent of current market value."}, {"id": "research-results-12", "text": "A two-state Markov-switching model assigns the high-volatility regime an unconditional probability of 0.23 and an expected duration of 18 weeks; the forecasting gains relative to the single-state specification are concentrated almost entirely in the four quarters containing regime transitions."}, {"id": "research-results-17", "text": "Alpha decays approximately linearly in assets under management and reaches zero near $3.1 billion under a 15% ADV participation cap and 210% annual two-way turnover; the estimate is sensitive to the impact exponent, falling to $1.7 billion under a linear-impact assumption."}, {"id": "postmortem-7", "text": "The staleness check keyed on the last packet arrival time for the channel rather than the last update time per symbol, so heartbeats and administrative messages kept the book marked healthy for all sixteen minutes."}, {"id": "rfc-doc-2", "text": "The 4 MiB part size is chosen deliberately: below 1 MiB, per-request overhead dominates for NovaSeq X CBCL trees (roughly 1.1 M files per 25B flowcell), and above 16 MiB the p99 on the 25 GbE uplink exceeds our 500 ms budget."}, {"id": "onboarding-handbook-13", "text": "Attribution is decomposed monthly into signal, sizing, timing, and execution. Where execution slippage exceeds 15% of gross signal P&L for a sleeve in any quarter, the sleeve is reviewed for capacity, not for alpha."}, {"id": "rfc-doc-14", "text": "Proposed checksum policy is two-tier: xxh3-128 computed inline at transfer for integrity, SHA-256 computed asynchronously and recorded within 24 hours for the archival manifest. Inline SHA-256 is not affordable at our 3.2 GB/s aggregate ingest."}, {"id": "postmortem-16", "text": "Sequence arithmetic in the arbitrator was widened to uint64 and a replay test now exercises a synthetic 2^31 rollover against captured UTP traffic; merged as PR #4471 and deployed to all four production gateways on 2026-07-21."}, {"id": "onboarding-handbook-12", "text": "A new signal trades in shadow for sixty consecutive sessions at full notional in the simulator before the Investment Committee will consider an allocation. The shadow clock does not reset for refactoring; it resets for any change to the signal itself."}, {"id": "research-results-10", "text": "Delta-hedged positions in S&P 500 index calls, rebalanced daily and held to maturity, lose an average of 0.42% of underlying notional per month (t = \u22126.13), consistent with a negatively priced volatility factor rather than with unhedged directional exposure."}, {"id": "comparison-memo-15", "text": "Runtime is a genuine constraint here, not a rounding error: the full nightly Greeks run takes 12 minutes under the analytic framework and just over four hours at 65,000 paths under the Monte Carlo alternative, which pushes delivery past the 06:30 ET cut-off the PM group works to."}, {"id": "comparison-memo-0", "text": "Cboe Europe's CXE book prices on maker-taker \u2014 0.30 bps to the aggressor against a 0.20 bps passive rebate \u2014 while Aquis charges a banded monthly subscription with no per-trade component; at the desk's 2025 average of \u00a362m traded per month in European mid-caps, the subscription option is cheaper by approximately \u00a341k annually."}]
const OFF=[0,9,18,27,5,14,23,32]
function rotate(l,k){const n=l.length,o=((k%n)+n)%n;return l.slice(o).concat(l.slice(0,o))}
const S={type:'object',properties:{labels:{type:'array',items:{type:'object',
 properties:{id:{type:'string'},fine:{type:'string'},status:{type:'string'},
             scope:{type:'string'},form:{type:'string'}},
 required:['id','fine','status','scope','form'],additionalProperties:false}}},
 required:['labels'],additionalProperties:false}
const books = `=== TYPE CODEBOOK ===\n${BOOK}\n=== END TYPE CODEBOOK ===\n\n` +
  `=== STATUS ===\n${STATUS}\n=== END STATUS ===\n\n` +
  `=== SCOPE ===\n${SCOPE}\n=== END SCOPE ===\n\n` +
  `=== FORM ===\n${FORM}\n=== END FORM ===\n\n`
const head = `You are applying a fixed codebook to short knowledge statements. Use ONLY the ` +
  `codebook below. Do not read any file, do not search.\n\nFor EVERY statement return one ` +
  `fine label, one status rung, one scope value and one form. They are independent. If no ` +
  `type applies return "general"; if no rung applies return "n/a".\n\n`
phase('Rate')
const jobs=[]
// arm SMALL: four calls of ten per rater
for (let r=0;r<8;r++){
  const ord=rotate(ITEMS,OFF[r])
  for (let b=0;b<4;b++){
    const chunk=ord.slice(b*10,(b+1)*10)
    jobs.push(()=>agent(head+books+`STATEMENTS (id, text):\n`+chunk.map(i=>`${i.id}\t${i.text}`).join('\n'),
      { label:`S-r${r}b${b}`, phase:'Rate', schema:S }
    ).then(res=>({arm:'SMALL', rater:r, labels: res?res.labels:null})))
  }
}
// arm BIG: all forty in one call per rater
for (let r=0;r<8;r++)
  jobs.push(()=>agent(head+books+`STATEMENTS (id, text):\n`+rotate(ITEMS,OFF[r]).map(i=>`${i.id}\t${i.text}`).join('\n'),
    { label:`B-r${r}`, phase:'Rate', schema:S }
  ).then(res=>({arm:'BIG', rater:r, labels: res?res.labels:null})))
const rated=await parallel(jobs)
return { items: ITEMS, raters: rated.filter(Boolean).filter(x=>x.labels) }
